#include <iostream>
#include <vector>

using namespace std;

int main() {
    long m;
    int n;
    cin >> m >> n;
    vector<int> desire(n);
    for(int i = 0; i < n; i++) cin >> desire[i];
    return 0;
}